import { ObtenerCancionesPipe } from './obtener-canciones.pipe';

describe('ObtenerCancionesPipe', () => {
  it('create an instance', () => {
    const pipe = new ObtenerCancionesPipe();
    expect(pipe).toBeTruthy();
  });
});
